'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import toast from 'react-hot-toast';
import { Bell, ChevronLeft, LockKeyhole, Mail, PackageCheck, ShieldCheck, Store, Trash2, UserRound, UsersRound } from 'lucide-react';
import LoadingSpinner from '@/components/ui/LoadingSpinner';
import DeleteAccountModal from '@/components/profile/DeleteAccountModal';
import { useAuth } from '@/context/AuthContext';
import { profileService } from '@/services/profileService';

const configs = { vendedor: { title: 'Configuración del vendedor', eyebrow: 'Panel de vendedor', description: 'Controla las alertas y avisos relacionados con tu tienda.', back: '/dashboard/vendedor', profile: '/dashboard/vendedor/perfil', defaults: { email_notifications: true, seller_orders: true, stock_alerts: true }, rows: [[Mail, 'Notificaciones por correo', 'Recibe avisos importantes sobre tu cuenta y tienda.', 'email_notifications'], [PackageCheck, 'Nuevos pedidos', 'Recibe una alerta cuando un cliente compre tus productos.', 'seller_orders'], [Bell, 'Alertas de inventario', 'Te avisaremos cuando un producto tenga poco stock.', 'stock_alerts']] }, admin: { title: 'Configuración del administrador', eyebrow: 'Panel de administración', description: 'Define cómo recibir avisos sobre la operación de la plataforma.', back: '/dashboard/admin', profile: '/perfil', defaults: { email_notifications: true, seller_applications: true, user_activity: true, system_alerts: true }, rows: [[Mail, 'Notificaciones por correo', 'Recibe resúmenes y avisos administrativos importantes.', 'email_notifications'], [Store, 'Solicitudes de vendedores', 'Recibe alertas cuando llegue una nueva solicitud.', 'seller_applications'], [UsersRound, 'Actividad de usuarios', 'Recibe avisos sobre cambios relevantes en las cuentas.', 'user_activity'], [ShieldCheck, 'Alertas del sistema', 'Mantente informado sobre incidencias y eventos críticos.', 'system_alerts']] } };

function Toggle({ checked, onChange, label }) { return <button type="button" role="switch" aria-checked={checked} aria-label={label} onClick={onChange} className={`flex h-8 w-[92px] shrink-0 items-center justify-between rounded-full p-1 ${checked ? 'bg-emerald-600' : 'bg-slate-300'}`}><span className={`text-[9px] font-extrabold ${checked ? 'order-1 pl-1 text-white' : 'order-2 pr-1 text-slate-600'}`}>{checked ? 'ACTIVO' : 'APAGADO'}</span><span className={`h-6 w-6 rounded-full bg-white shadow-md ${checked ? 'order-2' : 'order-1'}`} /></button>; }

export default function RoleSettings({ role }) {
  const router = useRouter();
  const { user, role: currentRole, isAuthenticated, loading: authLoading } = useAuth();
  const config = configs[role];
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [preferences, setPreferences] = useState(config.defaults);
  const [deleteOpen, setDeleteOpen] = useState(false);
  const [deleteReason, setDeleteReason] = useState('');
  const [deleteError, setDeleteError] = useState('');
  const [deleting, setDeleting] = useState(false);

  useEffect(() => {
    if (authLoading) return;
    if (!isAuthenticated || !user?.id || currentRole !== role) { router.replace(currentRole === 'admin' ? '/dashboard/admin/configuraciones' : '/dashboard/vendedor/configuraciones'); return; }
    let active = true;
    profileService.getProfile(user.id).then((result) => { if (!active) return; const saved = result.profile?.details?.preferences || {}; setPreferences(Object.fromEntries(Object.entries(config.defaults).map(([key, fallback]) => [key, saved[key] ?? fallback]))); setLoading(false); }).catch(() => { if (active) setLoading(false); });
    return () => { active = false; };
  }, [authLoading, config, currentRole, isAuthenticated, role, router, user?.id]);

  const save = async () => { setSaving(true); const result = await profileService.updateProfileDetails(user.id, { preferences }); setSaving(false); if (result.success) toast.success('Configuración guardada'); else toast.error(result.error || 'No se pudo guardar la configuración'); };
  const requestDeletion = async () => { if (!deleteReason.trim()) return setDeleteError('Cuéntanos por qué deseas eliminar tu cuenta.'); setDeleting(true); setDeleteError(''); try { const response = await fetch('/api/account/delete', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ reason: deleteReason.trim() }) }); const result = await response.json(); if (!response.ok || !result.success) throw new Error(result.error || 'No se pudo enviar la solicitud.'); setDeleteOpen(false); setDeleteReason(''); toast.success('Solicitud enviada para revisión'); } catch (error) { setDeleteError(error.message); } finally { setDeleting(false); } };

  if (!config || loading || authLoading) return <div className="flex min-h-screen items-center justify-center bg-slate-50"><LoadingSpinner size="lg" /></div>;
  return <main className="min-h-screen bg-slate-50 px-4 pb-20 pt-3 text-slate-800 sm:px-8 lg:px-12"><div className="mx-auto max-w-4xl"><Link href={config.back} className="mb-4 inline-flex items-center gap-2 text-xs font-bold text-slate-500 hover:text-slate-900"><ChevronLeft className="h-4 w-4" /> Volver al panel</Link><header className="mb-8"><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.22em] text-amber-600">{config.eyebrow}</p><h1 className="text-3xl font-extrabold tracking-tight text-slate-950 sm:text-4xl">{config.title}</h1><p className="mt-2 text-sm text-slate-500">{config.description}</p></header><div className="grid gap-6 lg:grid-cols-[1fr_260px]"><div className="space-y-6"><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-5"><h2 className="text-base font-bold text-slate-900">Preferencias de avisos</h2><p className="mt-1 text-xs text-slate-500">Activa o desactiva cada tipo de notificación.</p></div>{config.rows.map(([Icon, title, description, key]) => <div key={key} className="flex items-center justify-between gap-5 border-b border-slate-100 px-5 py-5 last:border-0"><div className="flex min-w-0 items-start gap-4"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-slate-100 text-slate-700"><Icon className="h-5 w-5" /></div><div><h3 className="text-sm font-bold text-slate-900">{title}</h3><p className="mt-1 text-xs leading-5 text-slate-500">{description}</p></div></div><Toggle checked={preferences[key]} onChange={() => setPreferences((current) => ({ ...current, [key]: !current[key] }))} label={title} /></div>)}<div className="flex justify-end border-t border-slate-100 px-5 py-4"><button type="button" onClick={save} disabled={saving} className="rounded-xl bg-slate-900 px-5 py-3 text-xs font-bold text-white disabled:opacity-50">{saving ? 'Guardando...' : 'Guardar configuración'}</button></div></section>{role !== 'admin' && <><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-5"><h2 className="text-base font-bold text-slate-900">Cuenta y seguridad</h2><p className="mt-1 text-xs text-slate-500">Administra tu acceso y la información de tu cuenta.</p></div><Link href={config.profile} className="flex items-center justify-between border-b border-slate-100 px-5 py-5 hover:bg-slate-50"><span className="text-sm font-bold text-slate-900">Información del perfil</span><span className="text-slate-400">›</span></Link><Link href="/actualizar-password" className="flex items-center justify-between px-5 py-5 hover:bg-slate-50"><span className="flex items-center gap-3 text-sm font-bold text-slate-900"><LockKeyhole className="h-5 w-5 text-slate-600" /> Contraseña y acceso</span><span className="text-slate-400">›</span></Link></section><section className="rounded-2xl border border-rose-200 bg-white p-5"><div className="flex items-center justify-between gap-4"><div className="flex items-center gap-3"><Trash2 className="h-5 w-5 text-rose-600" /><div><h2 className="text-sm font-bold text-slate-900">Zona de riesgo</h2><p className="mt-1 text-xs text-slate-500">Solicita eliminar permanentemente tu cuenta.</p></div></div><button type="button" onClick={() => { setDeleteOpen(true); setDeleteError(''); }} className="rounded-xl border border-rose-200 px-4 py-2.5 text-xs font-bold text-rose-600 hover:bg-rose-50">Eliminar cuenta</button></div></section></>}</div><aside className="h-fit rounded-2xl bg-[#162536] p-6 text-white shadow-lg"><h2 className="text-base font-bold">{role === 'admin' ? 'Control central' : 'Tu tienda, bajo control'}</h2><p className="mt-2 text-xs leading-5 text-white/65">Las preferencias se guardan en tu perfil.</p><div className="mt-5 border-t border-white/15 pt-4"><Link href="/privacidad" className="block text-xs font-semibold text-white/75 hover:text-white">Aviso de privacidad</Link><Link href="/terminos" className="mt-3 block text-xs font-semibold text-white/75 hover:text-white">Términos y condiciones</Link></div></aside></div></div>{deleteOpen && <DeleteAccountModal reason={deleteReason} error={deleteError} loading={deleting} onReasonChange={setDeleteReason} onClose={() => setDeleteOpen(false)} onSubmit={requestDeletion} />}</main>;
}
