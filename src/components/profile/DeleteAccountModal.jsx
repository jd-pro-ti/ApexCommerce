'use client';

import { Trash2 } from 'lucide-react';

export default function DeleteAccountModal({ reason, error, loading, onReasonChange, onClose, onSubmit }) {
  return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/50 p-4" role="dialog" aria-modal="true" aria-labelledby="delete-account-title"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="mb-4 flex items-start gap-3"><div className="rounded-xl bg-rose-50 p-2 text-rose-600"><Trash2 className="h-5 w-5" /></div><div><h2 id="delete-account-title" className="text-base font-bold text-slate-900">Eliminar cuenta</h2><p className="mt-1 text-sm leading-5 text-slate-500">Se eliminarán permanentemente tu perfil, información personal, productos y datos relacionados. Esta acción no se puede deshacer.</p></div></div><p className="mt-1 text-sm leading-5 text-slate-500">Tu solicitud será enviada al administrador. La cuenta no se eliminará hasta que sea revisada y aprobada.</p><label className="mt-4 block text-xs font-bold uppercase tracking-wide text-slate-600" htmlFor="delete-account-reason">¿Por qué deseas eliminar tu cuenta?</label><textarea id="delete-account-reason" value={reason} onChange={(event) => onReasonChange(event.target.value)} rows={4} maxLength={1000} placeholder="Escribe el motivo de tu solicitud..." className="mt-2 w-full resize-none rounded-xl border border-slate-300 px-4 py-3 text-sm text-slate-900 focus:border-rose-500 focus:outline-none focus:ring-2 focus:ring-rose-100" />{error && <p className="mt-3 rounded-lg bg-rose-50 p-3 text-xs text-rose-700">{error}</p>}<div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} disabled={loading} className="rounded-xl border border-slate-200 px-4 py-2.5 text-xs font-bold text-slate-600 hover:bg-slate-50">Cancelar</button><button type="button" onClick={onSubmit} disabled={loading} className="rounded-xl bg-rose-600 px-4 py-2.5 text-xs font-bold text-white hover:bg-rose-700 disabled:cursor-not-allowed disabled:opacity-50">{loading ? 'Enviando...' : 'Solicitar eliminación'}</button></div></div></div>;
}
